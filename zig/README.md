# README

## Workflow

* Run the tests
* Implement the `normalize` function until all tests are passing
* Refactor your implementation if needed
* Increase the value of `ENABLED_TESTS` (max. value is 9)
* Repeat until all 9 tests are passing

## Running the tests

```sh
zig test src/*
```
